/**
 * This is a simple test to see if mocha is working as expected.
 */
suite('The setup test', function() {
    'use strict';

    // 3rd party libs
    test('expects mocha to be working', function() {
        expect(true).to.be.true;
    });
});
